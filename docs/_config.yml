title: BREADS
description: >-
  BREADS.
baseurl: "/BREADS"
url: "https://feastorg.github.io"

theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag

markdown: kramdown
kramdown:
  input: GFM

# Don't publish these
exclude:
  - TODO.md
  - .indexignore
  - breads_planning.drawio

# Apply layout: page to all markdown under docs/
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
